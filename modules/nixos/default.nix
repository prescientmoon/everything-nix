# (https://nixos.wiki/wiki/Module).

{
  # example =  ./example.nix;
  cloudflared = ./cloudflared.nix;
  nginx = ./nginx.nix;
  pounce = ./pounce.nix;
  sqliteWeb = ./sqlite-web.nix;
  tuwunel = ./tuwunel.nix;
}
