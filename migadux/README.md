`migadux` is a tiny CLI for declaratively specifying
[migadu](https://migadu.com/) aliases

The CLI can either be run in `dry-run` or `sync` mode. The secrets are
automatically picked up from the `secrets.user.yaml` file (check
`secrets.example.yaml` for the structure).
